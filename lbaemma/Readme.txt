code folder includes R code.

plots folder is to put results generated by R folder.

.Rprofile, renv folder, and renv.lock are automatically created by library(renv) init().

lbaemma is a R project.